apiVersion: apps/v1
kind: Deployment
metadata:
  name: gymfrontendreact
  labels:
    app: gymfrontendreact
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gymfrontendreact
  template:
    metadata:
      labels:
        app: gymfrontendreact
    spec:
      containers:
      - name: gymfrontendreact
        image: rafdev0904/sportly:latest
        ports:
        - containerPort: 3000
        resources:
          limits:
            cpu: 500m
          requests:
            cpu: 200m
        
        
---

apiVersion: v1
kind: Service
metadata:
  name: sportly-svc
spec:
  selector:
    app: gymfrontendreact
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
    nodePort: 31147
